{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd src/main/capstone-ui && npm install --legacy-peer-deps && npm run build && cp -r dist/* ../resources/static/ && cd ../../../.. && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -Xms256m -Xmx512m -Dserver.address=0.0.0.0 -Dspring.profiles.active=railway -Djava.awt.headless=true -jar target/*.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
