{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd src/main/capstone-ui && rm -rf node_modules dist package-lock.json && npm cache clean --force && npm install --legacy-peer-deps && npm run build && ls -la dist && cp -r dist/* ../resources/static/ && ls -la ../resources/static && cd ../../../.. && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "java -Xms256m -Xmx512m -Dserver.address=0.0.0.0 -Dspring.profiles.active=railway -Djava.awt.headless=true -jar app.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
